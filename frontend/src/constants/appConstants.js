export const DATASET_OPTIONS = Object.freeze({
  DEFAULT: "default",
  EMPTY: "empty",
  UPLOADED: "uploaded",
});

export const VIEW_MODES = Object.freeze(["Day", "Week", "Month"]);

export const SORT_MODES = Object.freeze({
  ORIGINAL: "original",
  START: "start",
  END: "end",
});
